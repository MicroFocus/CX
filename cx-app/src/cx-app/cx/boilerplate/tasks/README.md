## Notes

### Task file names

There is no name normalization in Grunt. File names are loaded as tasks (strings). Therefore there cannot be
for example JS Style Guide compliant `options/usemin-prepare.js` file.
