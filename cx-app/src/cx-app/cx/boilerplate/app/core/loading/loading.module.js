(function () {
    "use strict";

    /**
     * @ngdoc module
     * @name app.core.loading
     * @memberof app.core
     * @summary
     *  This module contains the loading indicator that will show mostly during UI-Router resolves.
     */
    angular.module("app.core.loading", []);
})();
